Folder containing your projects.
Each time the launcher runs, a new project is created (except if a project in this forlder already has the same name).
Each project folder contains the config.yaml as it was set up when the launcher runned.